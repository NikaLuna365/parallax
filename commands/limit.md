---
name: limit
description: Alias for /parallax:limits
---

# `/parallax:limit`

This command is exactly an alias for `/parallax:limits`; it has no separate
collector or routing logic.

```bash
python3 scripts/provider-runtime.py limit [provider] [--json] [--watch N]
```
